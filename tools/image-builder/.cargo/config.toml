# Override parent workspace config — this tool runs on the HOST, not bare-metal
[build]
target = "x86_64-pc-windows-msvc"

[unstable]
# Explicitly empty — overrides parent's build-std which would cause duplicate core/alloc
